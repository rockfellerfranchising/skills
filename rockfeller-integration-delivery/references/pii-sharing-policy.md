# PII sharing

Send the minimum data needed for the provider's stated function. Prefer IDs over identity fields; never send CPF, payment details, auth material, audio, transcripts, or placement results unless the integration contract explicitly requires them and approval exists. Mask PII in logs, dashboards, dead letters, and support exports.
