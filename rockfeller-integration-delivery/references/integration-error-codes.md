# Integration error codes

Use stable upper snake case machine codes at the boundary: `INVALID_PAYLOAD`, `INVALID_SIGNATURE`, `UNAUTHORIZED_PROVIDER`, `DUPLICATE_EVENT`, `PROVIDER_TIMEOUT`, `PROVIDER_UNAVAILABLE`, `PROVIDER_REJECTED`, `DELIVERY_FAILED`, and `REPLAY_NOT_ALLOWED`. Keep actionable human messages separate from provider response bodies.
