# Group model

Use groups only for modeled business scopes. When a formal tenant or school model exists, use opaque `tenant` and `school` group keys with their canonical IDs. The inspected repositories do not yet provide this model; do not emit fabricated tenancy groups. Other groups require a documented owner and lifecycle.
